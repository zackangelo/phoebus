schema {
    query: Query
}

type Query {
    peopleCount: Int!
    person: Person!
}

interface Pet {
  name: String!
}

type Dog implements Pet {
    name: String!
}

type Cat implements Pet {
    name: String!
}

type Person {
    firstName: String!
    lastName: String!
    age: Int
    pets: [Pet!]!
}

enum DogBreed {
    RETRIEVER
    LAB
}

enum CatBreed {
    TABBY
    MIX
}
